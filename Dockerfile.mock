# Mock User Service Dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY mock_user_service.py .

RUN pip install --no-cache-dir fastapi uvicorn bcrypt httpx

EXPOSE 8001

CMD ["python", "mock_user_service.py"]
